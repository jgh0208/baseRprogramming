A <- sample(1:9,size = 4)
cond <- c(1,4,5,3)
A == cond
if(all(A == cond)){
  "correct!!"  
} else if(any(A == cond){
  ba <- A %in% cond
  st <- A == cond  
  if(((A %in% cond) == (A == cond)) == TRUE){
    N <- as.numeric(((A %in% cond) & (A == cond)) == TRUE)
    N
    sum(N)
    paste0(sum(N),"strike!!")
  } else if{
    if(((BA[[1]] == F) == (ST[[1]] == T)){
      p <- 1
    } else {
      p <- 0
    }
    p
    
    
  } else {
    "There is no strike and no ball."
  }